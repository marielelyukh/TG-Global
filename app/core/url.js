;(function () {
    angular
        .module('factory.url', [])
        .factory('url', [
            function () {
                var baseUrl = 'http://mahsevon.grassbusinesslabs.tk/api/';

                return {

                };
            }
        ]);
})();
